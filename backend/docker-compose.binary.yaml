services:
  backend:
    image: "kashin-binary:latest"
    container_name: "kashin-backend"
    build:
      context: .
      dockerfile: Dockerfile.binary
    ports:
      - "3030:3030"
    env_file:
      - .env.local

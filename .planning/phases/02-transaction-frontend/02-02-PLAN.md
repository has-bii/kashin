---
phase: 02-transaction-frontend
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - frontend/src/features/transaction/components/TransactionSheet.tsx
  - frontend/src/features/transaction/components/TransactionForm.tsx
  - frontend/src/features/transaction/components/TransactionDeleteDialog.tsx
autonomous: true
requirements: [TXN-01, TXN-02, TXN-03]

must_haves:
  truths:
    - "User can open a Sheet to create a new transaction"
    - "User can click a transaction row to edit it in the same Sheet"
    - "User can delete a transaction via alert-dialog inside the edit Sheet"
    - "Sheet form validates fields before submission"
    - "After create/edit/delete, the transaction list refreshes"
  artifacts:
    - path: "frontend/src/features/transaction/components/TransactionSheet.tsx"
      provides: "Mode-aware Sheet wrapper (create/edit)"
      exports: ["TransactionSheet"]
    - path: "frontend/src/features/transaction/components/TransactionForm.tsx"
      provides: "Form fields inside Sheet"
      exports: ["TransactionForm"]
    - path: "frontend/src/features/transaction/components/TransactionDeleteDialog.tsx"
      provides: "Alert dialog for delete confirmation inside edit Sheet"
      exports: ["TransactionDeleteDialog"]
  key_links:
    - from: "frontend/src/features/transaction/components/TransactionForm.tsx"
      to: "frontend/src/features/transaction/hooks/use-transaction-form.ts"
      via: "useTransactionForm hook provides form instance and mutation"
      pattern: "useTransactionForm"
    - from: "frontend/src/features/transaction/components/TransactionDeleteDialog.tsx"
      to: "frontend/src/features/transaction/hooks/use-delete-transaction.ts"
      via: "useDeleteTransaction hook handles delete mutation"
      pattern: "useDeleteTransaction"
    - from: "frontend/src/features/transaction/components/TransactionSheet.tsx"
      to: "frontend/src/features/transaction/components/TransactionForm.tsx"
      via: "Sheet renders TransactionForm inside SheetContent"
      pattern: "TransactionForm"
---

<objective>
Build the transaction create/edit Sheet form and delete confirmation dialog. After this plan, users can create, edit, and delete individual transactions.

Purpose: Delivers TXN-01 (create), TXN-02 (edit), TXN-03 (delete) as the primary CRUD UI.
Output: Three components — TransactionSheet, TransactionForm, TransactionDeleteDialog.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-transaction-frontend/02-CONTEXT.md
@.planning/phases/02-transaction-frontend/02-RESEARCH.md
@.planning/phases/02-transaction-frontend/02-01-SUMMARY.md
@.claude/skills/frontend-feature.md

<interfaces>
<!-- From Plan 01 outputs -->

From frontend/src/features/transaction/types/index.ts:
  export interface Transaction {
    id: string; userId: string; categoryId: string | null;
    type: "expense" | "income"; amount: string; currency: string;
    transactionDate: string; description: string | null; notes: string | null;
    source: string; createdAt: string; updatedAt: string;
    category: { id: string; name: string; type: TransactionType; icon: string; color: string } | null;
  }

From frontend/src/features/transaction/hooks/use-transaction-form.ts:
  export function useTransactionForm(args: { mode: "create"; onSuccess?: () => void } | { mode: "edit"; data: Transaction; onSuccess?: () => void }): { form, mutation }

From frontend/src/features/transaction/hooks/use-delete-transaction.ts:
  export function useDeleteTransaction(): { mutate, mutateAsync, isPending }

From frontend/src/features/transaction/validations/schema.ts:
  transactionCreateSchema, TransactionCreateDto

<!-- Existing UI components -->
From frontend/src/components/ui/sheet.tsx:
  Sheet, SheetContent, SheetHeader, SheetTitle, SheetDescription, SheetFooter, SheetClose

From frontend/src/components/ui/alert-dialog.tsx:
  AlertDialog, AlertDialogTrigger, AlertDialogContent, AlertDialogHeader, AlertDialogTitle,
  AlertDialogDescription, AlertDialogFooter, AlertDialogAction, AlertDialogCancel

From frontend/src/features/category/components/category-form.tsx:
  Canonical form pattern: form.Field with form.Subscribe for submit button disable state

From frontend/src/components/ui/select-tab.tsx:
  SelectTab component for expense/income type toggle (already used in CategoryForm)

From frontend/src/features/category/api/get-categories.query.ts:
  getCategoriesQueryOptions — needed for category dropdown in form
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build TransactionForm with all fields and category dropdown</name>
  <files>
    frontend/src/features/transaction/components/TransactionForm.tsx
  </files>
  <action>
    Create the form component rendered inside the Sheet. Follow `category-form.tsx` structure.

    **Props:** `{ mode: "create"; onSuccess?: () => void } | { mode: "edit"; data: Transaction; onSuccess?: () => void }`

    **Form fields (per D-05):**
    1. **Type** — Use `SelectTab` component (expense/income toggle), same as CategoryForm
    2. **Amount** — `Input` with `type="text"` (not number — avoids scroll increment). `inputMode="decimal"` for mobile numeric keyboard. Parse to number on submit via Zod coerce.
    3. **Transaction Date** — `Input` with `type="date"`. Default: today's date in create mode, existing date in edit mode.
    4. **Category** — `Select` dropdown. Fetch categories using `getCategoriesQueryOptions({ type: currentTypeValue })` so category list filters when type changes. Show category icon + name in each option. Allow null (no category).
    5. **Description** — `Input` with `maxLength={255}`. Optional.
    6. **Notes** — `Textarea`. Optional.

    **Behavior:**
    - Use `useTransactionForm` hook (from Plan 01) passing mode and data props.
    - `form.Subscribe` to disable submit button while submitting.
    - On submit success, `onSuccess` callback closes the sheet.
    - Per D-03 (edit mode only): Render `TransactionDeleteDialog` at the bottom of the form, below the submit button.

    **Anti-patterns to avoid:**
    - Do NOT use React.memo/useMemo/useCallback (React Compiler handles it)
    - Do NOT add staleTime/gcTime on the categories query
    - Import Zod from `"zod/v4"` if needed
  </action>
  <verify>
    <automated>cd /Users/hasbii/Desktop/projects/kashin/frontend && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>TransactionForm renders all 6 fields, uses SelectTab for type, fetches categories filtered by type, shows delete dialog in edit mode</done>
</task>

<task type="auto">
  <name>Task 2: Build TransactionSheet wrapper and TransactionDeleteDialog</name>
  <files>
    frontend/src/features/transaction/components/TransactionSheet.tsx,
    frontend/src/features/transaction/components/TransactionDeleteDialog.tsx
  </files>
  <action>
    1. **TransactionSheet.tsx** — Per D-01 and D-02, create a mode-aware Sheet.

    Props type (discriminated union):
    ```
    | { mode: "create"; open: boolean; onOpenChange: (v: boolean) => void }
    | { mode: "edit"; open: boolean; onOpenChange: (v: boolean) => void; data: Transaction }
    ```

    Render `Sheet` with `open` and `onOpenChange` props. Inside `SheetContent` with `side="right"`:
    - `SheetHeader` with `SheetTitle`: "Add Transaction" for create, "Edit Transaction" for edit
    - `SheetDescription`: brief context text
    - `TransactionForm` passing mode, data (edit only), and `onSuccess={() => onOpenChange(false)}`

    Per Pitfall 3 from research: the parent component manages `selectedTransaction` state. When `onOpenChange` receives `false`, the parent must reset `selectedTransaction` to null. This is the parent's responsibility — TransactionSheet just calls `onOpenChange(false)`.

    2. **TransactionDeleteDialog.tsx** — Per D-03, delete action inside the edit Sheet.

    Props: `{ transactionId: string; onSuccess?: () => void }`

    Use `AlertDialog` with:
    - Trigger: a destructive `Button` variant with "Delete Transaction" text
    - Content: warning title, description explaining deletion is permanent
    - Footer: Cancel button + Delete action button
    - On confirm: call `useDeleteTransaction().mutateAsync(transactionId)`, then `onSuccess`
    - Disable action button while `isPending`
  </action>
  <verify>
    <automated>cd /Users/hasbii/Desktop/projects/kashin/frontend && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>TransactionSheet opens as side panel with correct title per mode, TransactionDeleteDialog shows confirmation before deleting, both compile without errors</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes in frontend directory
- TransactionSheet accepts both create and edit mode props
- TransactionForm renders all required fields (type, amount, date, category, description, notes)
- Delete dialog only appears in edit mode (per D-03)
- Category dropdown fetches categories filtered by selected type
</verification>

<success_criteria>
- Sheet opens from the right side (D-01) with correct title per mode (D-02)
- Form validates via Zod before submission
- Create mutation calls POST /transaction, edit calls PUT /transaction/:id
- Delete confirmation dialog inside edit Sheet (D-03) calls DELETE /transaction/:id
- All mutations invalidate transaction queries on success
</success_criteria>

<output>
After completion, create `.planning/phases/02-transaction-frontend/02-02-SUMMARY.md`
</output>

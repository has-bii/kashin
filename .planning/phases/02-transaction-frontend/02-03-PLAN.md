---
phase: 02-transaction-frontend
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - frontend/src/features/transaction/components/TransactionList.tsx
  - frontend/src/features/transaction/components/TransactionFilterBar.tsx
  - frontend/src/features/transaction/components/TransactionPagination.tsx
  - frontend/src/features/transaction/components/TransactionListSkeleton.tsx
autonomous: true
requirements: [TXN-04, TXN-05]

must_haves:
  truths:
    - "User can see a paginated list of transactions with Date, Amount, Category, Type, Note columns"
    - "User can filter by type (All/Expense/Income), category, and date range"
    - "User can search transactions by description or notes text"
    - "Filters persist in the URL and update the list on change"
    - "Pagination controls navigate between server-side pages"
    - "Default date range on load is current month"
  artifacts:
    - path: "frontend/src/features/transaction/components/TransactionList.tsx"
      provides: "Custom transaction list with row rendering"
      exports: ["TransactionList"]
    - path: "frontend/src/features/transaction/components/TransactionFilterBar.tsx"
      provides: "Always-visible filter bar with type toggle, category select, date range, search"
      exports: ["TransactionFilterBar"]
    - path: "frontend/src/features/transaction/components/TransactionPagination.tsx"
      provides: "Server-side pagination controls"
      exports: ["TransactionPagination"]
  key_links:
    - from: "frontend/src/features/transaction/components/TransactionFilterBar.tsx"
      to: "frontend/src/features/transaction/hooks/use-transaction-filters.ts"
      via: "useTransactionFilters hook drives URL state"
      pattern: "useTransactionFilters"
    - from: "frontend/src/features/transaction/components/TransactionList.tsx"
      to: "frontend/src/features/transaction/api/get-transactions.query.ts"
      via: "useQuery with getTransactionsQueryOptions and resolved filter params"
      pattern: "getTransactionsQueryOptions"
    - from: "frontend/src/features/transaction/components/TransactionList.tsx"
      to: "frontend/src/features/transaction/components/TransactionSheet.tsx"
      via: "Row click sets selectedTransaction and opens Sheet in edit mode"
      pattern: "setSelectedTransaction|onRowClick"
---

<objective>
Build the transaction list, filter bar, and pagination components. After this plan, users can view, filter, search, and paginate their transactions.

Purpose: Delivers TXN-04 (filtered list) and TXN-05 (search) — the primary read UI.
Output: TransactionList, TransactionFilterBar, TransactionPagination, TransactionListSkeleton components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-transaction-frontend/02-CONTEXT.md
@.planning/phases/02-transaction-frontend/02-RESEARCH.md
@.planning/phases/02-transaction-frontend/02-01-SUMMARY.md
@.claude/skills/frontend-feature.md

<interfaces>
<!-- From Plan 01 outputs -->

From frontend/src/features/transaction/types/index.ts:
  export interface Transaction { id, type, amount (string), currency, transactionDate, description, notes, category: {...} | null, ... }
  export interface PaginatedTransactionResponse { data: Transaction[], total, page, limit, totalPages }

From frontend/src/features/transaction/hooks/use-transaction-filters.ts:
  export function useTransactionFilters(): { filters: { type, categoryId, dateFrom, dateTo, search, page }, setFilters, resolvedDateFrom, resolvedDateTo }
  // resolvedDateFrom/dateTo provide current month defaults when URL params are null (per D-09)

From frontend/src/features/transaction/api/get-transactions.query.ts:
  export function getTransactionsQueryOptions(params: GetTransactionsParams): QueryOptions

From frontend/src/features/category/api/get-categories.query.ts:
  export function getCategoriesQueryOptions(params): QueryOptions
  // Needed for category dropdown in filter bar

<!-- Existing UI components -->
From frontend/src/components/ui/select-tab.tsx:
  SelectTab — type toggle (expense/income/all), already used in category feature

From frontend/src/components/ui/badge.tsx, skeleton.tsx, empty-state.tsx:
  Available for list row badges, loading state, and empty state

From frontend/src/components/ui/input.tsx, select.tsx, button.tsx, popover.tsx:
  Form controls for filter bar

Note: Date range picker requires installing react-day-picker + date-fns.
Run: pnpm add react-day-picker date-fns
Then: pnpm dlx shadcn@latest add calendar (generates calendar.tsx in components/ui/)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install date picker deps and build TransactionFilterBar</name>
  <files>
    frontend/src/features/transaction/components/TransactionFilterBar.tsx
  </files>
  <action>
    **Setup (run first):**
    - `cd frontend && pnpm add react-day-picker date-fns`
    - `cd frontend && pnpm dlx shadcn@latest add calendar` (generates `components/ui/calendar.tsx` if not present)
    - Verify calendar component exists after installation

    **TransactionFilterBar.tsx** — Per D-08, always-visible filter bar above the list.

    Contains four filter controls in a horizontal row (flex, wrap on mobile):

    1. **Type toggle** — Three options: All (null), Expense, Income. Use `SelectTab` or a simple button group with `Badge` styling. When "All" is selected, type param is null (removed from URL). Changing type resets page to 1.

    2. **Category dropdown** — `Select` component. Fetch all categories via `getCategoriesQueryOptions({ type: null })` (all types). Show icon + name per option. Include "All Categories" as first option (null value). Changing category resets page to 1.

    3. **Date range picker** — Use `Popover` + `Calendar` from shadcn with `mode="range"` (react-day-picker range mode). Display selected range in the trigger button formatted via `date-fns` `format()`. Per D-09, default to current month (from `useTransactionFilters` resolved values). Changing dates resets page to 1.

    4. **Search input** — `Input` with search icon, placeholder "Search transactions...". Debounce input by 300ms before updating URL param. Changing search resets page to 1.

    Wire all controls to `useTransactionFilters` hook — each control reads from `filters` and writes via `setFilters`.

    **Anti-patterns to avoid:**
    - Do NOT add a second `NuqsAdapter` (already at root per Pitfall 6)
    - Do NOT use `data-table.tsx` (per D-04)
  </action>
  <verify>
    <automated>cd /Users/hasbii/Desktop/projects/kashin/frontend && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Filter bar renders with type toggle, category dropdown, date range picker, and search input; all controls update URL params via nuqs; page resets to 1 on filter change</done>
</task>

<task type="auto">
  <name>Task 2: Build TransactionList, pagination, skeleton, and wire to page</name>
  <files>
    frontend/src/features/transaction/components/TransactionList.tsx,
    frontend/src/features/transaction/components/TransactionPagination.tsx,
    frontend/src/features/transaction/components/TransactionListSkeleton.tsx,
    frontend/src/app/dashboard/(main)/transactions/page.tsx
  </files>
  <action>
    1. **TransactionListSkeleton.tsx** — Render 5-8 skeleton rows matching the list row height. Use `Skeleton` component from `@/components/ui/skeleton`.

    2. **TransactionPagination.tsx** — Server-side pagination controls.
    Props: `{ page: number, totalPages: number, onPageChange: (page: number) => void }`.
    Render Previous/Next buttons (disabled at boundaries) and page indicator (e.g., "Page 2 of 5"). Use `Button` variants. Use shadcn `Pagination` component if available, otherwise build simple prev/next.

    3. **TransactionList.tsx** — Per D-04, build a CUSTOM list (NOT data-table.tsx). Per D-05, columns: Date, Amount, Category, Type, Note.

    - Use `useTransactionFilters` to get resolved filter params
    - Pass params to `useQuery(getTransactionsQueryOptions({...resolvedParams}))` to fetch data
    - Show `TransactionListSkeleton` while loading (`isLoading`)
    - Show `EmptyState` when data is empty (use `empty-state.tsx`)
    - Render as a `<table>` with `<thead>` and `<tbody>` rows, or a flex/grid div list
    - Per D-07: Row click opens edit sheet — accept `onRowClick: (transaction: Transaction) => void` prop
    - **Date column:** Format with `date-fns` `format(new Date(transactionDate), "MMM dd, yyyy")`
    - **Amount column:** Format with `Intl.NumberFormat` for currency display. Color code: green for income, red for expense
    - **Category column:** Show category icon + name badge, or "Uncategorized" if null
    - **Type column:** Badge with "Expense" or "Income"
    - **Note column:** Truncate description to ~50 chars with ellipsis
    - Render `TransactionPagination` below the list, wired to `setFilters` for page changes

    4. **Update transactions/page.tsx** — Replace placeholder content with actual components:
    - Import and render `TransactionFilterBar` in a `Suspense` boundary
    - Import `TransactionList` (can use `dynamic` import with `TransactionListSkeleton` as loading)
    - Import `TransactionSheet` from Plan 02 (if available) or prepare the state management:
      - `selectedTransaction` state (Transaction | null)
      - `sheetOpen` state (boolean)
      - `sheetMode` derived: if selectedTransaction is set, "edit"; otherwise "create"
      - "Add Transaction" button in header opens sheet in create mode (per D-02)
      - Row click handler sets selectedTransaction and opens sheet in edit mode (per D-07)
      - Per Pitfall 3: reset selectedTransaction to null when sheet closes
    - Render `TransactionSheet` if Plan 02 components are available, otherwise add TODO comment
  </action>
  <verify>
    <automated>cd /Users/hasbii/Desktop/projects/kashin/frontend && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Transaction list renders rows with all 5 columns, pagination navigates pages via URL, skeleton shows during loading, empty state shown when no results, page wires filter bar + list + sheet together</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes in frontend directory
- `react-day-picker` and `date-fns` are in `frontend/package.json` dependencies
- `calendar.tsx` exists in `frontend/src/components/ui/`
- Filter bar updates URL params when controls change
- List re-fetches when URL params change (React Query key includes filter params)
- Pagination controls update page param in URL
- Default date range is current month (D-09)
</verification>

<success_criteria>
- User can filter by type, category, date range, and search text (D-08)
- Filters persist in URL (D-10) — refreshing page preserves filters
- Default date range is current month (D-09)
- Server-side pagination works (D-06)
- Row click triggers edit sheet opening (D-07)
- Custom list built without data-table.tsx (D-04)
- List shows Date, Amount, Category, Type, Note columns (D-05)
</success_criteria>

<output>
After completion, create `.planning/phases/02-transaction-frontend/02-03-SUMMARY.md`
</output>

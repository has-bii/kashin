---
phase: 02-transaction-frontend
plan: 04
type: execute
wave: 3
depends_on: ["02-02", "02-03"]
files_modified:
  - backend/src/modules/transaction/index.ts
  - backend/src/modules/transaction/service.ts
  - frontend/src/features/transaction/hooks/use-bulk-delete.ts
  - frontend/src/features/transaction/components/TransactionBulkToolbar.tsx
  - frontend/src/features/transaction/components/TransactionList.tsx
  - frontend/src/features/transaction/api/export-transactions.ts
  - frontend/src/app/dashboard/(main)/transactions/page.tsx
autonomous: false
requirements: [TXN-06, EXPORT-01]

must_haves:
  truths:
    - "User can select multiple transactions via checkboxes"
    - "User can bulk-delete all selected transactions"
    - "User can export the current filtered view as a CSV file"
    - "CSV export respects active filters (WYSIWYG)"
  artifacts:
    - path: "backend/src/modules/transaction/service.ts"
      provides: "exportAll method returning CSV string"
      contains: "exportAll"
    - path: "backend/src/modules/transaction/index.ts"
      provides: "GET /export endpoint returning text/csv"
      contains: "export"
    - path: "frontend/src/features/transaction/hooks/use-bulk-delete.ts"
      provides: "Bulk delete mutation + selected rows state"
      exports: ["useBulkDelete"]
    - path: "frontend/src/features/transaction/components/TransactionBulkToolbar.tsx"
      provides: "Floating toolbar with delete action when rows selected"
      exports: ["TransactionBulkToolbar"]
    - path: "frontend/src/features/transaction/api/export-transactions.ts"
      provides: "CSV export function using Axios blob download"
      exports: ["exportTransactionsCsv"]
  key_links:
    - from: "frontend/src/features/transaction/api/export-transactions.ts"
      to: "GET /api/transaction/export"
      via: "Axios blob request with filter params"
      pattern: "api\\.get.*\\/transaction\\/export.*blob"
    - from: "frontend/src/features/transaction/hooks/use-bulk-delete.ts"
      to: "POST /api/transaction/bulk-delete"
      via: "Axios post with selected IDs array"
      pattern: "api\\.post.*bulk-delete"
---

<objective>
Add bulk-select/bulk-delete functionality and CSV export. This requires a small backend addition (export endpoint) plus frontend UI for row selection and export.

Purpose: Delivers TXN-06 (bulk-delete) and EXPORT-01 (CSV export) — completing all Phase 2 requirements.
Output: Backend export endpoint, bulk delete hook/toolbar, CSV export function, fully wired transactions page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-transaction-frontend/02-CONTEXT.md
@.planning/phases/02-transaction-frontend/02-RESEARCH.md
@.planning/phases/02-transaction-frontend/02-01-SUMMARY.md
@.planning/phases/02-transaction-frontend/02-02-SUMMARY.md
@.planning/phases/02-transaction-frontend/02-03-SUMMARY.md
@.claude/skills/frontend-feature.md
@.claude/skills/backend-module.md

<interfaces>
<!-- Backend transaction service (will be modified) -->

From backend/src/modules/transaction/service.ts:
  export abstract class TransactionService {
    static async getAll(userId, query): Promise<{ data, total, page, limit, totalPages }>
    static async bulkDelete(userId, ids): Promise<{ count }>
    // exportAll to be added
  }

From backend/src/modules/transaction/query.ts:
  export const getAllQuery = t.Object({ page?, limit?, type?, categoryId?, dateFrom?, dateTo?, search? })

From backend/src/modules/transaction/index.ts:
  Elysia controller with routes. New /export route to be added BEFORE /:id routes.

<!-- Frontend types from Plan 01 -->

From frontend/src/features/transaction/types/index.ts:
  export interface Transaction { id, type, amount, transactionDate, description, notes, category, ... }

From frontend/src/features/transaction/hooks/use-transaction-filters.ts:
  export function useTransactionFilters(): { filters, setFilters, resolvedDateFrom, resolvedDateTo }

From frontend/src/features/transaction/api/get-transactions.query.ts:
  export function getTransactionsQueryOptions(params): QueryOptions
  export function getTransactionsQueryKey(params): QueryKey
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add backend CSV export endpoint</name>
  <files>
    backend/src/modules/transaction/service.ts,
    backend/src/modules/transaction/index.ts
  </files>
  <action>
    Per D-12 and Pitfall 1 from research: the backend needs a CSV export endpoint.

    1. **service.ts** — Add `exportAll` static method to `TransactionService`:
    - Signature: `static async exportAll(userId: string, query: GetAllQuery)`
    - Reuse the same `where` clause logic from `getAll` (type, categoryId, dateFrom, dateTo, search filters)
    - Do NOT paginate — fetch ALL matching records (no skip/take)
    - Build CSV string with headers: `Date,Type,Amount,Currency,Category,Description,Notes`
    - For each transaction row: format date as ISO date string, amount as number string, category as name or empty string, escape commas/quotes in description/notes
    - Return the CSV string

    2. **index.ts** — Add `GET /export` route:
    - Place it BEFORE `POST /bulk-delete` and `GET /:id` routes (same route ordering pattern from Phase 1 — static paths before parameterized)
    - Auth guard: `{ auth: true }`
    - Query validation: reuse `getAllQuery` (same filter params, page/limit ignored)
    - Set response headers: `Content-Type: text/csv`, `Content-Disposition: attachment; filename="transactions.csv"`
    - Return the CSV string from `TransactionService.exportAll(user.id, query)`
    - Use Elysia's response handling — return a `Response` object with appropriate headers
  </action>
  <verify>
    <automated>cd /Users/hasbii/Desktop/projects/kashin/backend && bun run dev &amp; sleep 3 && curl -s -o /dev/null -w "%{http_code}" http://localhost:3030/api/transaction/export 2>/dev/null; kill %1 2>/dev/null</automated>
  </verify>
  <done>GET /api/transaction/export returns CSV with correct headers, accepts same filter params as GET /api/transaction, fetches all matching records (not paginated)</done>
</task>

<task type="auto">
  <name>Task 2: Build bulk-select, bulk-delete, CSV export frontend, and wire to page</name>
  <files>
    frontend/src/features/transaction/hooks/use-bulk-delete.ts,
    frontend/src/features/transaction/components/TransactionBulkToolbar.tsx,
    frontend/src/features/transaction/api/export-transactions.ts,
    frontend/src/features/transaction/components/TransactionList.tsx,
    frontend/src/app/dashboard/(main)/transactions/page.tsx
  </files>
  <action>
    1. **hooks/use-bulk-delete.ts** — Manages selected row IDs and bulk delete mutation.
    - State: `selectedIds: Set<string>` (or string array)
    - Methods: `toggleId(id)`, `toggleAll(ids)`, `clearSelection()`, `isSelected(id)`, `selectedCount`
    - Mutation: `useMutation` calling `api.post("/transaction/bulk-delete", { ids: [...selectedIds] })`
    - On success: toast, clear selection, invalidate `["transactions"]` queries

    2. **components/TransactionBulkToolbar.tsx** — Floating toolbar that appears when `selectedCount > 0`.
    - Shows: "{N} selected" text + "Delete Selected" destructive button
    - Delete button opens an `AlertDialog` confirmation: "Delete {N} transactions? This cannot be undone."
    - On confirm: call bulk delete mutation
    - Position: fixed/sticky at bottom of list area, or as a bar above/below the list (Claude's discretion)

    3. **api/export-transactions.ts** — CSV download function.
    - Per D-12 and research Pattern 4: use `api.get("/transaction/export", { params: filters, responseType: "blob" })`
    - Create object URL, trigger download via anchor element click, revoke URL
    - Accept filter params to send current active filters
    - Export as named function: `exportTransactionsCsv(filters)`

    4. **Modify TransactionList.tsx** — Add checkbox column for bulk select:
    - Add header checkbox (select all on current page) and per-row checkboxes
    - Wire to `useBulkDelete` hook's toggle methods
    - Keep row click for edit sheet (D-07) — checkbox click should NOT open edit sheet (stopPropagation on checkbox)

    5. **Update transactions/page.tsx** — Final wiring:
    - Per D-11: Add Export button in page header next to "Add Transaction" button. On click: call `exportTransactionsCsv` with current resolved filters. Show loading spinner while exporting.
    - Render `TransactionBulkToolbar` when selections exist
    - Ensure all state (sheet, filters, bulk selection) is properly managed at page level
  </action>
  <verify>
    <automated>cd /Users/hasbii/Desktop/projects/kashin/frontend && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Checkboxes appear on each row, bulk toolbar shows when rows selected, bulk delete removes selected rows, Export button downloads CSV of current filtered view</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete transaction management UI end-to-end</name>
  <files>none</files>
  <action>
    Human verification of the complete transaction management UI. All automated work is done in Tasks 1-2 and prior plans. This checkpoint confirms visual and functional correctness.
  </action>
  <what-built>Complete transaction management UI — create/edit Sheet form, delete confirmation, filtered list with search and pagination, bulk-select with bulk-delete, CSV export</what-built>
  <how-to-verify>
    1. Start both servers: `cd backend && bun run dev` and `cd frontend && pnpm dev`
    2. Navigate to http://localhost:3000/dashboard/transactions
    3. Verify sidebar shows "Transactions" entry
    4. Click "Add Transaction" — Sheet opens from right (D-01)
    5. Fill form: type=expense, amount=50000, date=today, pick a category, add description — Save
    6. Verify new transaction appears in list
    7. Click the row — Sheet opens in edit mode with pre-filled data (D-02)
    8. Change amount — Save — verify list updates
    9. Click row again — click "Delete Transaction" inside Sheet — confirm — verify removed (D-03)
    10. Create 3+ transactions, then test filters: type toggle, category dropdown, date range, search (D-08)
    11. Verify URL updates with filter params (D-10)
    12. Refresh page — verify filters persist from URL
    13. Select 2+ rows via checkboxes — verify bulk toolbar appears
    14. Click "Delete Selected" — confirm — verify rows removed (TXN-06)
    15. Click "Export" button in header — verify CSV file downloads (EXPORT-01)
    16. Apply a filter, then export — verify CSV only contains filtered data (D-12)
  </how-to-verify>
  <verify>
    <automated>echo "Manual verification checkpoint — no automated command"</automated>
  </verify>
  <done>All 16 verification steps pass — transaction CRUD, filters, bulk-delete, and CSV export work end-to-end</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Backend: `GET /api/transaction/export` returns CSV with headers
- Frontend: `npx tsc --noEmit` passes
- Bulk select checkboxes render on each row
- Bulk delete removes selected transactions
- Export downloads CSV matching current filters
- All Phase 2 success criteria from ROADMAP met
</verification>

<success_criteria>
- User can select multiple transactions and bulk-delete them (TXN-06)
- User can export filtered transactions as CSV (EXPORT-01, D-11, D-12)
- CSV export sends active filters to backend (D-12) — not just current page data
- Checkbox click does not trigger row edit (separate from D-07 row click)
- All 7 requirements (TXN-01 through TXN-06 + EXPORT-01) are fully functional end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/02-transaction-frontend/02-04-SUMMARY.md`
</output>

---
phase: 03-dashboard-backend
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - backend/src/modules/dashboard/service.ts
autonomous: true
requirements:
  - DASH-02
  - DASH-04

must_haves:
  truths:
    - "GET /api/dashboard/summary returns totalIncome, totalExpense, netBalance as numbers for the requested date range"
    - "When dateFrom/dateTo are omitted, summary defaults to current month"
    - "GET /api/dashboard/recent returns last N transactions with nested category object"
    - "Recent transactions are ordered by transactionDate descending"
  artifacts:
    - path: "backend/src/modules/dashboard/service.ts"
      provides: "Implemented summary() and recent() methods"
      contains: "prisma.$transaction"
  key_links:
    - from: "backend/src/modules/dashboard/service.ts"
      to: "prisma.transaction.aggregate"
      via: "summary method"
      pattern: "prisma\\.transaction\\.aggregate"
    - from: "backend/src/modules/dashboard/service.ts"
      to: "prisma.transaction.findMany"
      via: "recent method"
      pattern: "prisma\\.transaction\\.findMany"
---

<objective>
Implement the summary and recent transactions service methods with real Prisma queries. Summary uses atomic two-aggregate read for income/expense totals. Recent reuses categoryInclude pattern.

Purpose: Deliver DASH-02 (income vs expenses balance) and DASH-04 (recent transactions widget) backend data.
Output: Two fully functional service methods in DashboardService.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-dashboard-backend/03-CONTEXT.md
@.planning/phases/03-dashboard-backend/03-RESEARCH.md
@.planning/phases/03-dashboard-backend/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From backend/src/modules/dashboard/service.ts (after Plan 01):
```typescript
function resolveMonthRange(dateFrom?: string, dateTo?: string) {
  // Returns { gte: Date, lte: Date }
}

export abstract class DashboardService {
  static async summary(userId: string, query: SummaryQuery) { /* stub */ }
  static async recent(userId: string, limit: number) { /* stub */ }
}
```

From backend/src/modules/transaction/service.ts (categoryInclude shape to reuse):
```typescript
const categoryInclude = {
  category: { select: { id: true, name: true, type: true, icon: true, color: true } },
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement DashboardService.summary()</name>
  <read_first>
    - backend/src/modules/dashboard/service.ts
    - backend/src/modules/transaction/service.ts
    - backend/prisma/schema.prisma
  </read_first>
  <files>backend/src/modules/dashboard/service.ts</files>
  <action>
Replace the `summary` stub in `DashboardService` with a real implementation per D-01 and D-04:

1. Call `resolveMonthRange(query.dateFrom, query.dateTo)` to get the `transactionDate` filter (defaults to current month when params absent per D-04).

2. Use `prisma.$transaction([...])` for atomic two-aggregate read (same pattern as `TransactionService.getAll`):
   - First aggregate: `prisma.transaction.aggregate({ where: { userId, type: "income", transactionDate: dateRange }, _sum: { amount: true } })`
   - Second aggregate: same but `type: "expense"`

3. Convert Decimal to number per D-01:
   - `const totalIncome = parseFloat((incomeResult._sum.amount ?? 0).toString())`
   - `const totalExpense = parseFloat((expenseResult._sum.amount ?? 0).toString())`

4. Return `{ totalIncome, totalExpense, netBalance: totalIncome - totalExpense }`

The method signature stays: `static async summary(userId: string, query: SummaryQuery)`.
  </action>
  <verify>
    <automated>cd /Users/hasbii/Desktop/projects/kashin/backend && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `service.ts` contains `prisma.$transaction([` inside the `summary` method
    - `service.ts` contains `type: "income"` and `type: "expense"` in two separate aggregate calls
    - `service.ts` contains `parseFloat(` for Decimal-to-number conversion (at least 2 occurrences)
    - `service.ts` contains `resolveMonthRange(query.dateFrom, query.dateTo)` inside `summary`
    - Return object has keys `totalIncome`, `totalExpense`, `netBalance`
    - `summary` method includes `userId` in every `where` clause
  </acceptance_criteria>
  <done>summary() returns { totalIncome: number, totalExpense: number, netBalance: number } using atomic two-aggregate read with Decimal-to-number conversion and current-month default.</done>
</task>

<task type="auto">
  <name>Task 2: Implement DashboardService.recent()</name>
  <read_first>
    - backend/src/modules/dashboard/service.ts
    - backend/src/modules/transaction/service.ts
  </read_first>
  <files>backend/src/modules/dashboard/service.ts</files>
  <action>
Replace the `recent` stub in `DashboardService` with a real implementation per D-03:

1. Define a `categoryInclude` const at module scope in dashboard's service.ts (same shape as transaction's):
   ```typescript
   const categoryInclude = {
     category: { select: { id: true, name: true, type: true, icon: true, color: true } },
   }
   ```

2. Implement the method:
   ```typescript
   static async recent(userId: string, limit = 5) {
     return prisma.transaction.findMany({
       where: { userId },
       include: categoryInclude,
       orderBy: { transactionDate: "desc" },
       take: limit,
     })
   }
   ```

The method returns the last `limit` transactions ordered by `transactionDate` descending, with nested category data. No date filtering — returns the most recent regardless of date range.
  </action>
  <verify>
    <automated>cd /Users/hasbii/Desktop/projects/kashin/backend && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `service.ts` contains `const categoryInclude` with `category: { select: { id: true, name: true, type: true, icon: true, color: true } }`
    - `recent` method contains `prisma.transaction.findMany`
    - `recent` method contains `orderBy: { transactionDate: "desc" }`
    - `recent` method contains `take: limit`
    - `recent` method contains `where: { userId }`
    - `recent` method contains `include: categoryInclude`
    - `bun run typecheck` passes with exit code 0
  </acceptance_criteria>
  <done>recent() returns last N transactions with nested category object, ordered by transactionDate desc, scoped to userId.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API | Query params (dateFrom, dateTo, limit) from untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Info Disclosure | summary() | mitigate | `userId` filter on both aggregate queries — no cross-user totals |
| T-03-06 | Info Disclosure | recent() | mitigate | `userId` filter on findMany — only returns user's own transactions |
| T-03-07 | Tampering | dateFrom/dateTo params | mitigate | Typebox validates as `t.String()`; `new Date()` constructor handles invalid strings gracefully (returns Invalid Date which Prisma rejects) |
| T-03-08 | Denial of Service | recent() | mitigate | `limit` bounded to maximum 50 via Typebox schema in query.ts (Plan 01) |
</threat_model>

<verification>
1. `cd backend && bun run typecheck` exits 0
2. `summary()` uses `prisma.$transaction` with two aggregates and `parseFloat` conversion
3. `recent()` uses `findMany` with `categoryInclude`, `orderBy`, and `take`
4. Both methods filter by `userId`
</verification>

<success_criteria>
- summary() returns computed totals as numbers (not Decimal/string)
- summary() defaults to current month when date params omitted
- recent() returns transactions with nested category data
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-backend/03-02-SUMMARY.md`
</output>
